Simply checking if set size is the same, if it is then we return false.

we can also do it without mapping list to set by putting elements into set seen while iterating, if there is some that seen contains i then we break the loop.